# vim config

```bash
git clone https://github.com/lext-7/vimx.git && cd vimx && sh -x ./install.sh
# without YouCompleteMe
git clone https://github.com/lext-7/vimx.git && cd vimx && sh -x ./install.sh --simple-mode
```
